{
  "name": "camp-scheduler-ui",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "serve": "tsc --watch --preserveWatchOutput & python3 -m http.server 8000",
    "build": "tsc"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
